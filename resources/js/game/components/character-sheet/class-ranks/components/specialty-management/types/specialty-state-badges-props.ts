export default interface SpecialtyStateBadgesProps {
  is_damage: boolean;
  is_equipped: boolean;
  is_mastered: boolean;
  is_in_progress: boolean;
  is_available: boolean;
  is_locked_level: boolean;
  required_class_level: number;
}
