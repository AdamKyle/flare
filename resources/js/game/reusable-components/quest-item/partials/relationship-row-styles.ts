/**
 * Shared compact row styling for Quest Item factual relationship rows
 * (Quests, Locations, Monsters). A subtle bounded row rather than a heavy
 * Card, so a list of relationships reads as a scannable list of rows.
 */
export const relationshipRowClassName =
  'border-glacier-200 dark:border-glacier-800 bg-glacier-50 dark:bg-glacier-900/40 rounded-md border px-3 py-2';
