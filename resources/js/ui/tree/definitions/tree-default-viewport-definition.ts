/**
 * Resolved React Flow viewport for the Tree's default position: used
 * identically for the initial Tree render and the Reset View control so
 * both always agree.
 */
export default interface TreeDefaultViewportDefinition {
  x: number;
  y: number;
  zoom: number;
}
