export enum LogsDashboardMessages {
  LoadFiles = 'Could not load log file list.',
  LoadEntries = 'Could not load log entries.',
  LoadDetail = 'Could not load the selected log detail.',
  Poll = 'Could not poll log entries.',
}
